1. наскільки я зрозумів, то в repositories мусять бути дані, а так як не потрібно працювати з жодною базою даних з умови, то це оз

2. зробив not found middleware

3. я вирішив, що при створенні нової нотатки буде показуватися інфа про неї

4. validation change: in separate file, types of fields should be taken from SingleNote type

5. Хоч і перевіряти чи є вписане id в req.params не є обов'язковим, проте для певності я створив валідатори там теж